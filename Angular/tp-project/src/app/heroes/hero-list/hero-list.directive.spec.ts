import { HeroListDirective } from './hero-list.directive';

describe('HeroListDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroListDirective();
    expect(directive).toBeTruthy();
  });
});
